{% extends "ScontrolBundle::layout.html.twig" %}

{% block title %}Descripción Log{% endblock %}

{% block area %}

<h2 align="left">Descripción de Log</h2>

<table class="vime-table-des" align="center">
    <tbody>
        <tr>
            <th align="left">Id</th>
            <td>{{ entity.id }}</td>
        </tr>
        <tr>
            <th align="left">Usuario</th>
            <td>{{ entity.gbusua.nombre }}</td>
        </tr>
        <tr>
            <th align="left">Fecha</th>
            <td>{{ entity.fecha|date('Y-m-d') }}</td>
        </tr>
        <tr>
            <th align="left">Módulo</th>
            <td>{{ entity.modulo }}</td>
        </tr>
        <tr>
            <th align="left">Acción</th>
            <td>{{ entity.accion }}</td>
        </tr>
    </tbody>
</table>

	<form id="vime-form" action="{{ path('gblogr_delete', { 'id': entity.id }) }}" method="post">
		{{ form_widget(delete_form) }}
	</form>

{% endblock %}

{% block action %}
<div class="vime-action">
	<table>
		<tr>
			<td>&nbsp;&nbsp;&nbsp;</td>
			<td onClick="go('{{ path('gblogr') }}')"><img src="{{ asset('bundles/scontrol/imagenes/iconos/listar.png') }}" title="Listar"></td>
			<td onClick="gId('vime-form').submit()"><img src="{{ asset('bundles/scontrol/imagenes/iconos/eliminar.png') }}" title="Eliminar"></td>
			<td onClick="go('{{ path('gblogr_edit', { 'id': entity.id }) }}')"><img src="{{ asset('bundles/scontrol/imagenes/iconos/editar.png') }}" title="Editar"></td>
		</tr>
	</table>
</div>
{% endblock %}

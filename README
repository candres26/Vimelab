Software De Gestion De Historias Clinicas Para VimeLab SL.

Favor crear las carpetas app/cache y app/logs.

$ mkdir app/cache
$ mkdir app/logs
# chmod -R 777 app/cache
# chmod -R 777 app/logs

En caso de no tener el archivo app/config/parameters.ini, debe crearse con el siguiente contenido:

[parameters]
    database_driver="pdo_mysql"
    database_host="host"
    database_port=""
    database_name="db"
    database_user="user"
    database_password="pass"
    mailer_transport="smtp"
    mailer_host="localhost"
    mailer_user=""
    mailer_password=""
    locale="es"
    secret="--hash generado--"

No se incluye este archivo ya que cada uno tiene datos de conexion diferente.

Mejorando estandares JavaScript y TWIG, grandes cambios en el modelo de funciones, en base.html.twig (movida a src) y layout.html.twig.

* Asitente De Ubicaciones Terminado, control de paises, províncias y ciudades.
* Tabla GbVars Agregada, para alojar datos de procesos y asistentes.
* Esquema de vistas para asistentes mejoradas.
* Integracion de asistentes con formularios maestros lograda usando IFRAMES.
* Asistente de seguridad casi listo, creacion de personal y usuarios.
* Modelo de varibles para ACL Diseñado.
* Archivos SQL de control; modelo.sql contiene la estructura de la DB, base.sql contiene la estructura y los datos m{inimos para comenzar (usuario root), db.sql contiene datos de prueba volatiles, vars.sql contiene los datos minimos de las variables (aun en construcción). 
